# Run Tests
  cwd=$(pwd);
  cd test
  phpunit --stop-on-failure .
  cd $cwd
  RESULT=$?
  [ $RESULT -ne 0 ] && printf "\n!! Some test are failing. If you're SURE you want to commit, run 'git commit --no-verify'\n\n" && exit 1

exit 0
